#define NDEBUG

#include <bits/stdc++.h>

using namespace std;

#ifdef NDEBUG
#  define debug(...)
#else
#  include "debug.hh"
#endif

int static_init = []() {
  ios_base::sync_with_stdio(false), cin.tie(0), cout << fixed;
  return 0;
}();

int main() {
  return 0;
}
