#define NDEBUG

#include <algorithm>
#include <cassert>
#include <cinttypes>
#include <cstdio>

#ifdef NDEBUG
#  define pr(...)
#else
#  define pr(...) printf(__VA_ARGS__)
#endif

using namespace std;

int main() {
  return 0;
}
